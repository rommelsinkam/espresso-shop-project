apiVersion: v1
kind: Service
metadata:
  name: {{ include "yan.fullname" . }}
  labels:
    {{- include "yan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yan.selectorLabels" . | nindent 4 }}
