apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "s3yan-espresso-shop-web.fullname" . }}-test-connection"
  labels:
    {{- include "s3yan-espresso-shop-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "s3yan-espresso-shop-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
