{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: serviceAccount
metadata:
  name: {{ include "s3yannick-espresso-shop-web.serviceAccountName" . }}
  labels:
    {{- include "s3yannick-espresso-shop-web.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
